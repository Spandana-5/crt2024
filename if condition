checking the if condition
#include <stdio.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if (b==0){
        printf("second cannot be zero");
        b=1;
    }
    int c=a/b;
    printf("\n%d",c);

    return 0;
}
