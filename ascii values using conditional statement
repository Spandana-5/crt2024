finding ascii values of lower,upper ,digit
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    char ch;
    scanf("%c",&ch);
    if(ch>=65&&ch<=90)
        printf("Upper");
    else if(ch>=97&&ch<=120)
        printf("Lower");
    else if(ch>=48&&ch<=57)
        printf("Number");
    else
        printf("Symbol");
    
    
    return 0;
}
